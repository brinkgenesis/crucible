{
  "name": "@crucible/bridge",
  "version": "0.0.1",
  "description": "Node subprocess that wraps the Claude Agent SDK for Crucible runs",
  "private": true,
  "type": "module",
  "main": "dist/sdk-port-bridge.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/sdk-port-bridge.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.86",
    "yaml": "^2.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
