# Copy to secret.yaml and fill in values. Never commit secret.yaml.
apiVersion: v1
kind: Secret
metadata:
  name: infra-orchestrator-secrets
type: Opaque
stringData:
  database-url: "ecto://user:password@postgres:5432/infra_orchestrator_prod"
  secret-key-base: "REPLACE_WITH_64_CHAR_SECRET"
  api-key: "REPLACE_WITH_API_KEY"
